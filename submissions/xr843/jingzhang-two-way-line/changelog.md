# 方案迭代记录

## v1.7 - 2026-08-10

- **五张图全部消除空白带**。此前逐图逐行扫描发现：`site-overview` 自身下半 30% 稀疏（行60–90% 仅 1.6/1.8/0.0%），而它是全包出现次数最多的一张图（正文§1、离线报告、展示页、A3 第三页、A0 第一板都用它）；另有四张图在行80–90% 存在同一条系统性裂口——那是内容底边与页脚注之间的固定间隙。本轮逐图重排后，**五张图近空行带全部清零**，平均墨迹 9.8% → 11.3%。
- **`site-overview` 墨迹 5.0% → 10.6%（翻倍）**。根因是走廊为窄长南北带，在等比例约束下把 66% 宽的图框横向留白过半（内容只落在图宽 15–45%）。主图轴收窄至 33.5% 宽后内容填满，腾出的右侧 60% 宽新增两块内容：**「明线／暗线双层剖面」概念示意**（地面遗址公园、上方 7.8 km 慢行绿脊、下方清华园隧道 6020 m、三处道口贯穿，并明确标注「非工程剖面，不表达工程线位或结构结论」）与**「三站一门·节点定位」**面板；中列图例下方新增**「八口·东西缝合点与其正交道路」**表，如实标出 K8 清河湾位于范围外 85 米。
- **`mobility-bluegreen` 右列改为三段叠放**（图例／路网等级构成表／双河冷环示意），补入按 EPSG:4548 实测的路网等级表：快速路2条7.0km、主干路5条12.6km、次干路5条14.1km、支路2条4.4km、慢行主径7.8km，并重申本方案不新增机动车道路、不拓宽既有红线。
- `land-use-structure` 与 `metrics-evidence` 分别通过下压主图轴、上延证据链条带补齐裂口。
- 四份 PDF 随图重建；四门自检重跑全过，`review_status` 仍为 `formal-review-ready`。

## v1.6 - 2026-08-10

- **补第七类用户画像「高龄与行动不便居民」**：此前六类画像里没有一类面向弱势群体，而 S5 老年就医陪导与 S9 视障领航两个班次本就是为其而设——画像表与班次表对不上。画像表同时新增「直接服务其的班次」一列，使每类画像都能指认出至少一个为其而设的班次与一处空间落点。`user_persona_count` 6 → 7，展示页与 A3 图册同步。
- **补包容性的三条硬约束**：非AI等价路径写入协议并逐条检验、下行回报中面向弱势与一线人群的班次占 5/12（逾四成）、受影响者本身有叫停权（S5 含参与居民、S9 含参与用户）——三条都可在 `visual/assets/twoway-runbook.json` 逐字段读到，不是承诺式表述。
- **§3.5 未来城市形态由三个命题扩为五个**，每个都绑定包内可核对的图层、指标或班次：算力下沉为街区设施、空间供给从整栋转向班次（时段×路段）、留白成为换向储备（18.6公顷=众智园的9.7%）、感知设施改变街道的设计对象（新增「可关停的公共传感点位」要素）、治理机制本身成为规划工具（以可执行检验定义用地适配性）。
- **§4.1 补指标状态分类**：36项指标中32项 `known`（附 `formula` 可复算）、4项 `unknown`（法定容积率、官方高度管控、人才密度基线、AI创新指数基线，均写明 `reason`、值为 `null`）。「对开指数」框架全部属于第三类，只给口径不给基线数值。
- **§4.2 补分期逻辑**：三期面积配比 176.9 / 249.7 / 169.3 公顷是被更新策略决定的——把最大的一期放在中间而非开头，因为第一期的公共骨架必须先证明「缝合确实带来公共收益」，第二期的组团更新才有谈判基础；这一顺序是对开协议「先给下行回报、再取上行资源」在时间维度上的复写。
- **§5 引言补密度梯度总述**，把三处重点区从「同一模板的三次复制」明确为由北到南的院区型→街区型→城市型梯度。
- **修正两处本轮自己写错的地方**：①§4.1 初稿称「33项 known」，实为32项；②§4.5 在具体高度数字旁引用了 `building_height_max_m`，而该指标状态为 `unknown`——已改为明示「概念体量统计值不得反向充当高度管控依据」。
- ⚠️**视觉闸门抓到一处确定性校验器漏掉的不一致**：`user_persona_count` 改为 7 后，离线展示页的 data-metric 仍写 6，`visual_review` 判 FAIL。已修，并同步 A3 图册的画像表与标题。
- 中英正文等义同步，report HTML 与四份 PDF 全部重建，四门自检重跑全过。

## v1.5 - 2026-08-10

- **A0 展板重排，消除空白版面**：此前两张展板的非背景像素仅占 4.6% 与 7.5%，且各有整幅近空行带（板一顶部中段与底部五分之一、板二顶部中段为 0.0%）。本轮把空白带全部换成实内容，两板均已无近空行带。
- 板一新增：**「双箭同轨」标识与释义**（与 `assets/two-way-line-logo.svg` 同一几何，直接绘制，不引外部图）；**底部整幅「三处重点区密度梯度」表**（面积、概念建筑栋数、概念建面、概念毛容积率、首位用地、空间性格六列，呈现院区型0.75→街区型0.96→城市型1.68）；**「两处范围事实·主动披露」**段，把清河湾道口越界 85 米与清华园车站旧址西偏 389 米直接印在展板上，而非藏在正文里。
- 板二新增：**「对开协议·可复算的准入契约」**块，含六条规则标签、桌面推演结果（12/12 可排图、6/6 变异被对应规则拦下）与可复现命令行；场景卡表由 8 条补满为 **12 条**。
- **修复两处图面缺陷**：①命令行原指定 DejaVu Sans，导致「零外部依赖」的「赖」字缺字形渲染成方块，已改回中文字体；②六条规则标签原排为一行，第三个已被板缘裁切，已改为按可用宽度自动换行并加断言防止再次越界。
- 图纸重建后 A0 体积由 6.8MB 降至 5.6MB，包体积 19.5MB → 17.2MB。
- **新增三条假设登记**：清河湾道口跨界、清华园车站旧址跨范围、四例案例出处无法核实，均写入 `assumptions.json`（6 条 → 9 条），使此前只存在于正文的披露进入机读证据层。
- 构建管线可复现性已验证：修改前先跑一遍，四个 PDF 与仓库内文件逐字节一致。

## v1.4 - 2026-08-10

- **来源登记从 14 条扩到 25 条，且每条都经过实际核验**：新增仓库内六份标准快照（公告正文、智能体任务书、用地用海分类、控规深度、城市设计管理办法、建筑深度登记条目），使正文每一处 `[standard:]` 主张都能回到一份可逐句回读的文件；新增三例案例的官方出处（Kendall Square Association、STATION F、JTC one-north），均实测返回 200 并核对了发布主体；新增本包原创的标识与机读契约两项资产登记。
- **对四例未能核实的案例如实降级**：国王十字官网对本包抓取返回 403、多伦多项目原运营方站点现已重定向至其它产品、张江站点未能确认发布主体、粤海街道无稳定官方出处——按「无法核实即不声称已清权」的原则，这四例保留为公开知识的定性概括，**不登记为可引用来源**，并在 §3.4 明写原因。
- **§4.5 城市风貌**改以包内 244 栋概念体量的实际高度分布为准（21.0–67.2 米，中位 37.8；24 米以下 4%、24–45 米 71%、45–60 米 19%、60 米以上 6%），替换此前「遗产界面12–24米、东侧干道45–80米」这一与图层不符的分区宣称，并按功能给出高度序列。
- **主动披露两处范围事实**（复算发现，非评审指出）：①八处道口中的**清河湾道口位于总体设计范围北缘之外约 85 米**，其余七处在范围内——该处的任务正是把明线接出边界缝到清河水岸，缝合动作必然跨界，故其实施依据落在统筹研究范围层面、不计入范围内用地与指标复算；②**清华园车站旧址位于总体设计范围以西约 389 米**，在统筹研究范围内，故 §4.5 对该处的文保引导属研究范围层面建议，班次 S8 的场所亦为跨范围的仪式性场所，其余十一张班次卡节点均在范围内。
- 中英正文等义同步，report HTML 重新渲染；四门自检重跑全过；`sources.json` 25 条与正文 `[source:]` 引用双向完整、无悬空无遗漏。

## v1.3 - 2026-08-10

- **三处重点区详细设计写到可复算深度**：§5.1/5.2/5.3 补入按 EPSG:4548 逐块求交复算的用地构成（三区交集面积分别精确等于192.1/104.3/72.0公顷）、概念建筑栋数与功能构成、层数与高度区间、区内路网长度与绿地面积。三区呈现**院区型（概念毛容积率0.75）→街区型（0.96）→城市型（1.68）**的密度梯度，作为「上行取绿电算力、下行还场景消费」的空间论证，而非随意分配。
- **修正正文与几何图层的两处不一致**：§5.1 原写「8至12层」、§5.3 原写「10至16层」，与 `buildings.geojson` 实际的 6–12 层与 8–16 层不符，已按图层更正。
- **§4.3 交通、轨道、市政与配套**由一句话（101字符）补为完整章节：路网等级结构与实测长度（快速路2条7.0km、主干路5条12.6km、次干路5条14.1km、支路2条4.4km）、八处道口与正交道路的对应关系、三处既有轨道站的站城一体边界、市政与配套口径。明确本方案不新增机动车道路、不拓宽既有红线。
- **§3.1 区域协同**由 426 字符补为四段：补充「为什么这段关系必须由海淀表达」的论证、清华园隧道6020米作为双层走廊的物理基底，并**明确限定该判断的边界**——不主张任何跨区域产业转移、投资或算力调度政策。
- **§12 风险版权合规**补入逐项资产权属表（几何、底图、五图、标识、机读契约与脚本、字体、政策来源各自的来源与许可），并说明隐私红线是由 schema 的数据类别枚举在契约层强制、而非仅靠承诺。
- **修正版权声明的一处失准**：v1.2 已交付绘制完成的标识，而 `report/copyright_statement.md` 仍写着「Logo 仅给出方向描述」。已更正为原创绘制声明，并补入新增标识、脚本与字体的权属与许可。
- 新增三项可复算指标（三处重点区概念建面）；概念毛容积率不登记为指标，以免与已登记为 `unknown` 的法定 `floor_area_ratio` 口径冲突。
- 中英正文等义同步，`report/proposal.html` 与 `report/proposal.en.html` 已重新渲染；四门自检重跑全过。

## v1.2 - 2026-08-10

- **Logo 落地为可缩放矢量图**：§3.2 此前只有文字描述的「双箭同轨」方向，现绘制为 `assets/two-way-line-logo.svg`（彩色）与 `assets/two-way-line-logo-mono.svg`（单色，`currentColor`）。两版几何一致，无文字、无外链、无未授权字体商标；离线展示页双语页首各引入一次。
- **对开协议从散文变为可复算契约**：新增 `visual/assets/twoway-protocol.schema.json`（班次凭证机读契约）、`twoway-runbook.json`（十二张真实班次卡 + 六个必须被拒的变异班次）、`run_twoway_tabletop.js`（Node 18+，无外部依赖的桌面推演）与 `twoway-tabletop-evidence.json`（本包记录的运行结果）。
- **闸门做了变异测试，不是摆设**：推演除检验十二张卡是否满足协议六条规则外，另要求六个变异班次各自被其对应规则拦下，否则同样判失败；实测把真实班次的运营主体清空、或把隐私红线翻转，脚本立即判红并以非零码退出。本包记录结果为 12/12 可排图、6/6 变异被拦下。
- **补全协议第三要素**：§6.2 的班次表原本只公布节点与上下行清单，而协议要求「节点、运营主体、上下行清单」三要素齐全方可排图。§6.3 新增十二张卡的运营主体与叫停主体指派表，依本方案自设的运行图管委会、三站乘务组、两台协调人架构编制，并明示全部为概念性运营建议、非既有机构或已批准的行政安排。
- 中英正文等义同步，`report/proposal.html` 与 `report/proposal.en.html` 已用 `scripts/render_proposal_html.py` 重新渲染。
- 未改动几何、指标数值、图纸与许可声明；未触碰其它投稿包、`submissions-data.js` 与 `gallery-publication.json`。

## v1.1 - 2026-08-10

- 持久化本包在当前 main 提交上的真实四门自检结果（deterministic validation / spatial review / visual packaging / professional evidence 均为 PASS），并写入 `review_status=formal-review-ready` 与 `manifest.validation_claim.readiness_contract=persisted-self-check-v1`。响应 #883 测得的存量缺口与 #807 的 readiness 契约。
- 证据由 `scripts/self_check_submission.py --mark-self-checked` 实跑生成并回读，内嵌各门原始报告；这是作者侧的可复现回放记录，不是独立背书，provenance 仍以受信 CI 或维护者重跑为准。
- 保留本包原有的六项包级自检（BOUNDARY_TRUST / KEY_AREAS_TRUST / LAND_USE_TOPOLOGY / METRIC_RECALC / VISUAL_STATIC / BILINGUAL_PAIRING）与四门规范化记录并存，使 `compliance_matrix.json` 的 `self_check_ids` 引用保持完整。
- 三条 `KEY_AREA_PROVISIONAL` 仍如实保留为 minor、非阻断提示：重点区为推定范围，不是官方红线，也不构成精确面积依据。
- 未改动几何、指标数值、图纸、正文结论或许可声明；未改动 `submissions-data.js`、`gallery-publication.json`，也未触碰其它投稿包。

## v1.0 - 2026-08-09

- 初版 formal 概念方案包：「京张对开 THE TWO-WAY LINE」。
- 自行推导街道贴合临时边界：以公告文字四至的 OSM 街道中线重建走廊多边形，EPSG:4548 下校准至公告 11.40 km²（偏差 +0.0006%）；三处重点区以公开轨道站点与文保单位位置锚定，面积校准至公告值 ±0.01% 内。
- 披露 maintainer 登记临时边界整体东偏约 600 米的对照发现（清华园车站旧址、遗址公园已建段、五道口/大钟寺站、古钟博物馆均落在其外），计划以 Issue 提交社区复核；本包与登记边界同为 provisional，非官方红线。
- 生成九层几何（189 块全覆盖用地分区，缺口约 658 m²；244 处概念建筑；14 条现状路网；明线 7.8 km；三期分期）与 29 项复算指标；spatial review PASS。
- 中英双语报告（proposal.md / proposal.en.md，v2 + 双语契约 v1）、五图双语十张、A3 文册与 A0 展板双语四册、离线 visual 双语两页。
- 十二班次场景卡（含三个产业测试验证）、六类画像、四处朝圣地标、七个全球案例、十四项更新项目、对开协议与运行图制度设计。
- 待办：向社区提交边界偏移 Issue；官方红线发布后整包重算。
